<p id="notice"><%= notice %></p>

<h2 class="govuk-heading-xl">Pages</h2>

<%= link_to 'Add a top level Page', new_content_page_path %>
<br/><br/>

<div>

  <% @content_pages.top_level.each_with_index do |content_page, index| %>

    <%= render partial: 'details_for_a_single_page', locals: {page_to_render: content_page, level: 1} %>

  <% end %>
</div>
<br>

<%= link_to 'Add a new Page', new_content_page_path %>
