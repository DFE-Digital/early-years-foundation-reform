<nav class="app-subnav">
  <% ContentPage.top_level.each do |top_level_page| %>
    <a class="<%= subnav_link_css_classes(top_level_page) %>" href="<%= content_url + '/' + top_level_page.slug %>">
      <h4 class="app-subnav__theme"><%= top_level_page.title %></h4>
    </a>
    <ul class="app-subnav__section ">
      <% top_level_page.children.each do |child| %>
        <li class=" app-subnav__section-item ">
          <a class="<%= subnav_link_css_classes(child) %>" href="<%= content_url + '/' + child.slug %>"><%= child.title %></a>
        </li>
      <% end %>
    </ul>
  <% end %>
</nav>
